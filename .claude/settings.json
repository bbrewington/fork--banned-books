{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\"file_path\":.*data/raw/'; then echo 'Blocked: raw data files are immutable source documents' >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}